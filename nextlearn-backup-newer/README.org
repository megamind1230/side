#+TITLE: nextlearn
#+DESCRIPTION: Learn anything. Fight distraction.
#+DATE: <2026-06-04 Thu>

* Description

nextlearn is a distraction-free desktop app for learning anything
through bite-sized decks. You write decks as ~.md~ files with YAML
frontmatter in ~~/nextlearn/decks/~, the app reads them, and you
flip through pages like a slideshow. Built with Avalonia UI + EF Core
SQLite.

This repo contains only ~nextlearn.Desktop~ — the offline Avalonia UI
desktop app. The companion website (for browsing/downloading decks)
is a separate project.

* Features

| Feature                         | Status     | Note                                                                               |
|---------------------------------+------------+------------------------------------------------------------------------------------|
| Fully offline                   | ✅ Done    | No network calls ever                                                              |
| File-based decks (.md / .org)   | ✅ Done    | YAML frontmatter + `#` / `##` headings                                             |
| Frontmatter metadata            | ✅ Done    | ~title:~, ~description:~, ~tags:~ in --- block                                     |
| Org #+TITLE:/#+DESCRIPTION:     | ✅ Done    | ~#+TITLE:~ / ~#+DESCRIPTION:~ / ~#+TAGS:~ in .org                                  |
| Tag system from frontmatter     | ✅ Done    | ~tags: health, morning, sleep~                                                     |
| #tag search (incremental)       | ✅ Done    | ~#st~ matches ~study~ (prefix match)                                               |
| Regex search toggle             | ✅ Done    | ☐ Regex checkbox switches title/desc/name + tags to regex                         |
| Search by title/desc/filename   | ✅ Done    | Plain text tokens search all three                                                 |
| Page-by-page learning           | ✅ Done    | Prev/Next with keyboard shortcuts                                                  |
| Section breadcrumb              | ✅ Done    | Shows ~# Section → ## Page~ with heading markers                                   |
| Rendered markdown content       | ✅ Done    | NativeWebView (HTML rendering via data URI)                                        |
| Code syntax highlighting        | ✅ Done    | highlight.js embedded, 52 langs w/ alias support                                   |
| Table rendering (pipe tables)   | ✅ Done    | Markdown + Org table syntax → HTML <table>                                         |
| .org file headings (~~** ~~)    | ✅ Done    | Parser regex matches ~*~/~~~** ~~ and ~#~/~~~## ~~                                 |
| .org code blocks (#+BEGIN_SRC)  | ✅ Done    | ~#+BEGIN_SRC lang~ / ~#+END_SRC~ → <pre><code>                                     |
| Inline code protection          | ✅ Done    | ~[text](url)~ inside backticks renders literally                                   |
| Code-block-aware page splits    | ✅ Done    | ~#~ inside code blocks don't trigger page breaks                                   |
| Deterministic deck identity     | ✅ Done    | SHA256 of file path → stable GUID                                                  |
| Dark mode theming               | ✅ Done    | #1E293B cards, amber Name: line                                                    |
| Selection color                 | ✅ Done    | Blue bg (#2563EB) + white text in WebView                                          |
| vim-style j/k scroll            | ✅ Done    | J/K scrolls home deck list, study box content                                      |
| h/l horizontal scroll           | ✅ Done    | H/L scrolls study box content left/right                                           |
| Inline search (no popup)        | ✅ Done    | `/` focuses bar, Esc unfocuses                                                     |
| Sidebar (☰ → settings, info)    | ✅ Done    | Backdrop click + Esc to close                                                      |
| Settings overlay page           | ✅ Done    | ⚙ in sidebar, placeholder content                                                 |
| Marketplace                     | ✅ Done    | Opens ~https://google.com~ in browser                                              |
| Get Plugins                     | ✅ Done    | Opens ~https://github.com/megamind1230~ in browser                                 |
| Stable progress across restarts | ✅ Done    | DeckFileIdentity + DeckService save                                                |
| Auto-refresh on file change     | ✅ Done    | FileSystemWatcher watches decks dir                                                |
| Streak tracking & daily log     | ✅ Done    | Wall-clock minutes, daily activity                                                 |
| Heatmap overlay (full-page)     | ✅ Done    | Snake layout 90° CCW, 6-level orange, green today                                  |
| Keyboard shortcuts              | ✅ Done    | N/P/J/K/H/L/Q/D/E//Esc (see table below)                                           |
| Settings (sidebar → ⚙)         | ✅ Done    | Theme, font, decks path, save/reset                                                |
| Theme switching                 | 🚧 Plugin  | Will be extracted (Dark/Light)                                                     |
| Tag cloud / tag filter          | ❌ Not yet | Planned (P27)                                                                      |
| Image/video content types       | ❌ Not yet | Models exist, UI missing                                                           |
| Spaced repetition algorithm     | ❌ Not yet | Basic 24h review only                                                              |
| Image rendering (markdown)      | ✅ Done    | ~![]()~ and ~![[path]]~ → inline image in WebView                                  |
| Go to page (Ctrl+G)             | ✅ Done    | Jump to any page by number during study                                            |
| Text zoom (Ctrl+Shift++/_)      | ✅ Done    | Font-only zoom, keeps component layout                                             |
| Image rendering (org)           | ✅ Done    | ~![[file:path]]~ / ~![[file:path][alt]]~                                           |
| Image overlay toolbar           | ✅ Done    | ◀▶ nav, zoom slider, invert, open in viewer                                        |
| Archive / Pin decks             | ✅ Done    | Rename-based: ~.md~~ / ~+.md~, stable progress IDs; "Learn" button on pinned cards |
| Open decks folder               | ✅ Done    | File manager fallback (17 binaries) → xdg-open                                     |
| Clickable links (http/https)    | ✅ Done    | ~[text](url)~ / ~[[url]]~ / ~[[url][text]]~ → <a>                                  |
| Bare URL auto-linking           | ✅ Done    | ~https://...~ auto-linked in both .md and .org                                     |
| Non-http URLs shown as-is       | ✅ Done    | ~[text](local/path)~ → raw text, not broken link                                   |
| Org [[...]] placeholder protect     | ✅ Done    | Prevents italic regex from corrupting file paths                                   |
| Quote blocks (> syntax)         | ✅ Done    | Renders `>` lines as <blockquote> in md and org                                    |
| Org block rendering (#+BEGIN_*) | ✅ Done    | #+BEGIN_QUOTE etc → monospace pre with dark bg                                     |

* Tech Stack

- .NET 10.0, Avalonia UI 11.3.12 (cross-platform desktop)
- CommunityToolkit.Mvvm 8.2.1 (source-generated MVVM)
- Entity Framework Core 8.0.0 + SQLite
- Serilog (file + console logging to ~~/nextlearn/log.txt~)
- NativeWebView 0.1.0-alpha.3 + NativeWebView.Platform.Linux (WebView HTML rendering)
- highlight.js 11.11.1 (syntax highlighting, 52 languages w/ alias support, embedded as Assets/custom-highlight.js)
- Linux: requires ~libwpewebkit-2.0~, ~libwpe-1.0~, ~libWPEBackend-fdo-1.0~
- Wayland: auto-detects ~XDG_SESSION_TYPE=wayland~, unsets ~WAYLAND_DISPLAY~
  (GLFW skip) + forces X11 backend via ~UseSkia().UseX11()~
- Markdown files as deck source (YAML frontmatter + `#` headings)

* Architecture

** Design philosophy — fully offline

The desktop app NEVER makes HTTP/S calls. Downloads are always
manual: user → website → download → drop into local folder.

** Data Sources

- ~~/.config/nextlearn/nextlearn.db~ — users, progress, activity
- ~~/nextlearn/decks/*.md~ / ~*.org~ — deck content with YAML frontmatter

** Clean Code Initiative

Phase 5 of the clean code initiative is complete:
- 5a: Naming conventions (async suffix, private field style)
- 5b: Dead code removal (flashcard, feedback, editor, search)
- 5c: Split MainWindow.axaml/.cs (~700 lines extracted into 8 UserControls, KeyboardHandler, WebViewBridge)
- 5d: Split service files (HtmlContentBuilder → strategy pattern; DeckService → DB + file layers)
- 5e: Service interfaces + DI preparation (IDeckService, IUserService, IDeckFileService, ISettingsService, IHtmlContentBuilder)

** Project Structure

| Path                            | Purpose                               |
|---------------------------------+---------------------------------------|
| ~nextlearn.Desktop/~            | Avalonia UI desktop app (C#, .NET 10) |
| ~nextlearn.Desktop/Models/~     | Deck, Page, Flashcard, User, etc.     |
| ~nextlearn.Desktop/Data/~       | AppDbContext (SQLite)                 |
| ~nextlearn.Desktop/Services/~   | Business logic layer                  |
| ~nextlearn.Desktop/ViewModels/~ | MVVM view models                      |
| ~nextlearn.Desktop/Views/~      | MainWindow.axaml (single window)      |

** Key Services

| Service             | Role                                        |
|---------------------+---------------------------------------------|
| DeckFileParser      | Static parser: frontmatter + heading → Deck |
| DeckFileIdentity    | SHA256 of path → deterministic GUID         |
| DeckService         | DB layer: decks, pages, progress tracking   |
| DeckFileService     | File ops: archive/pin/rename decks          |
| UserService         | Current user, daily activity, streak (computed from study minutes) |
| SettingsService     | Persists theme/font/paths to JSON           |
| HtmlContentBuilder  | Static HTML builder from Page model         |
| MarkdownInlineRenderer | Strategy for .md inline rendering        |
| OrgInlineRenderer   | Strategy for .org inline rendering          |

* Deck File Format

All ~.md~ files go in ~~/nextlearn/decks/~. Each file is a single deck.
The parser reads YAML frontmatter and section headings:

** With frontmatter (recommended)

#+BEGIN_SRC markdown
---
title: Wake Up Properly
description: Fix morning face puffiness with these simple changes.
tags: health, morning, sleep, face, self-care
---

# Section Title
Content here...

## Page Title
Page content here...

## Another Page
More content...
#+END_SRC

** .org file metadata

#+BEGIN_SRC org
#+TITLE: My Deck
#+DESCRIPTION: Description text here.
#+TAGS: tag1, tag2

* Section
** Page One
Content
#+END_SRC

When metadata is absent, the study page header falls back to the
disk file name (e.g. ~my-deck.org~). The deck list card shows the
first content line as title and 2nd+3rd lines as description.

** No frontmatter (fallback)

#+BEGIN_SRC markdown
# Deck Title (becomes the deck title)
First non-empty line after title (becomes description)

## Page One
Content

## Page Two
Content
#+END_SRC

** Heading rules
- `# Section` → sets section context (shown in breadcrumb: ~DeckTitle → Section~)
- `## Page` → creates a page with ~SectionTitle~ from nearest parent `#`
- No `##` headings → `#` headings become pages directly (legacy)

* Keyboard Shortcuts

| Key          | Context        | Action                                 |
|--------------+----------------+----------------------------------------|
| Ctrl + ,     | Global         | Open Settings                          |
| ?            | Global         | Toggle Shortcuts Handbook              |
| Esc          | Any overlay    | Close current overlay                  |
| Ctrl + G     | Learning       | Go to page (opens page-number dialog)  |
| n / Right    | Learning       | Next page                              |
| p / Left     | Learning       | Previous page                          |
| j            | Learning       | Scroll content down 40px               |
| k            | Learning       | Scroll content up 40px                 |
| h            | Learning       | Scroll content left 40px               |
| l            | Learning       | Scroll content right 40px              |
| j            | Home           | Scroll deck list down 40px             |
| k            | Home           | Scroll deck list up 40px               |
| q / d        | Any            | Go home (saves progress)               |
| q / d        | Settings       | Close settings + go home               |
| o            | Global         | Open decks folder in file manager      |
| `g` then `i` | Home           | Focus inline search bar (clear search) |
| `/`          | Home           | Focus inline search bar                |
| Click image  | Learning       | Open image in floating overlay         |
| Ctrl + = / - | Image overlay  | Zoom in / out                          |
| Ctrl + 0     | Image overlay  | Reset zoom to 1:1                      |
| N / P / ◀ ▶  | Image overlay  | Next / prev image on current page      |
| Button: Done | Last page      | Complete deck + navigate home          |
| Key: n       | Last page      | Complete but stay (allows revision)    |
| Ctrl+Shift++ | Global         | Text zoom in (larger)                  |
| Ctrl+Shift+_ | Global         | Text zoom out (smaller)                |
| Ctrl+Shift+0 | Global         | Reset text zoom / reset heatmap zoom   |
| Ctrl+Shift+= | Heatmap open   | Heatmap zoom in                        |
| Ctrl+Shift+- | Heatmap open   | Heatmap zoom out                       |
| 🔥 Streak    | Home / Sidebar | Open heatmap overlay                   |

* How to Use

1. Create a ~.md~ file in ~~/nextlearn/decks/~ with YAML frontmatter:
   #+BEGIN_SRC markdown
   ---
   title: My Deck
   description: What this deck is about
   tags: topic1, topic2, keyword
   ---
   #+END_SRC

2. Add sections and pages:
   #+BEGIN_SRC markdown
   # Section Name
   ## Page One
   Content for this page...
   #+END_SRC

3. Run the app — decks appear on the home view
4. Search by title, description, filename, or ~#tag~ (prefix matching; enable ☐ Regex for regex patterns)
5. Click a deck or press N to start learning
6. Navigate pages with ← → or N / P keys
7. Press Q / D to go home (progress auto-saved)

* Link & Image Syntax Reference

Every link and image syntax variant and how the app handles each:

** Markdown (.md) syntax

| Syntax                              | Type    | Renders as                          | Rule                              |
|-------------------------------------+---------+-------------------------------------+-----------------------------------|
| ~[text](http://url)~                | Link    | ~<a data-href="url">text</a>~       | http/https only                   |
| ~[text](local/path)~               | Link    | ~[text](local/path)~ (raw text)     | Non-http → as-is                  |
| ~[[http://url]]~                    | Link    | ~<a data-href="url">url</a>~        | Wiki-style, http/https only       |
| ~[[local/text]]~                   | Link    | ~[[local/text]]~ (raw text)         | Non-http → as-is                  |
| ~![alt](img.png)~                   | Image   | ~<img class="inline-image">~        | Always renders image              |
| ~![alt](img.png "title")~          | Image   | ~<img>~ (title stripped)            | Title via ~""~ or ~&quot;~        |
| ~![[path]]~                         | Image   | ~<img class="inline-image">~        | Obsidian-style image              |
| ~[![alt](img.png)](url)~           | Mixed   | ~<img>~ inner, outer raw            | Link regex skips ~[...](~         |
| ~https://bare.url~                  | Link    | ~<a data-href="url">url</a>~        | Auto-linked in both md & org      |
| ~\`[text](url)\`~                  | Code    | ~<code>[text](url)</code>~          | Protected by code span extraction |
| ~- [x] Done~ / ~TODO~ / ~DONE~    | Meta    | Colored spans                       | Checkboxes + inline keywords      |

** Org (.org) syntax

| Syntax                                | Type    | Renders as                          | Rule                              |
|---------------------------------------+---------+-------------------------------------+-----------------------------------|
| ~[[http://url][text]]~                | Link    | ~<a data-href="url">text</a>~       | http/https only                   |
| ~[[http://url]]~                      | Link    | ~<a data-href="url">url</a>~        | http/https only                   |
| ~[[file:path][alt]]~                  | Text    | ~[[file:path][alt]]~ (raw text)     | No ~!~ → as-is                    |
| ~[[file:path]]~                       | Text    | ~[[file:path]]~ (raw text)          | No ~!~ → as-is                    |
| ~[[local/target][text]]~             | Text    | ~[[local/target][text]]~ (raw)      | Non-http → as-is with brackets    |
| ~[[local/target]]~                    | Text    | ~[[local/target]]~ (raw)            | Non-http → as-is with brackets    |
| ~![[http://url][text]]~              | Link    | ~<a data-href="url">text</a>~       | http overrides ~!~                |
| ~![[http://url]]~                     | Link    | ~<a data-href="url">url</a>~        | http overrides ~!~                |
| ~![[file:path][alt]]~                | Image   | ~<img class="inline-image">~        | Strip ~file:~, alt preserved      |
| ~![[file:path]]~                      | Image   | ~<img class="inline-image">~        | Strip ~file:~, no alt             |
| ~![[path][alt]]~                      | Image   | ~<img class="inline-image">~        | No ~file:~, path used directly    |
| ~![[path]]~                           | Image   | ~<img class="inline-image">~        | No ~file:~, path used directly    |
| ~[](img.png)~                         | Image   | ~<img>~                             | Markdown-style image (same as md) |
| ~[text](http://url)~                  | Link    | ~<a>~                               | Markdown-style link (same as md)  |
| ~[text](local/path)~                 | Link    | ~[text](local/path)~ (raw text)     | Non-http → as-is                  |
| ~/italic/~ / ~*bold*~ / ~~code~~    | Format  | ~<em>~ / ~<strong>~ / ~<code>~     | Inline formatting                 |
| ~[[...]]~ / ~![[...]]~ in path       | Protect | Placeholder before formatting       | Prevents / italic path corruption |
| ~https://bare.url~                    | Link    | ~<a data-href="url">url</a>~        | Auto-linked                       |

** Image resolution rules

All image paths resolve relative to ~$DECKS_DIR~ (~~/nextlearn/decks/~):

| Syntax                      | Resolved path                                      |
|-----------------------------+----------------------------------------------------|
| ~![alt](img.png)~           | ~$DECKS_DIR/img.png~                               |
| ~![alt](subdir/img.png)~    | ~$DECKS_DIR/subdir/img.png~                        |
| ~![[file:./sub/img.png]]~   | ~$DECKS_DIR/sub/img.png~ (strips ~file:~)          |
| ~http://example.com/cat.png~ | ~<a>~ link (not an image)                          |

Path traversal (e.g. ~../../etc/passwd~) is blocked by the path-traversal
guard in ~RenderImageTag()~ — renders as ~image path mis-referenced~ error.

** Image error messages

| Condition                      | Display                               |
|--------------------------------+---------------------------------------|
| ~imageDir~ not configured     | ~image folder not configured: path~   |
| Path escapes ~$DECKS_DIR~     | ~image path mis-referenced: path~     |
| File does not exist            | ~image not found: path~               |
| File read/embed failure        | ~image not found: path~ (catch block) |
| URL passed to image syntax     | ~<a data-href="url">alt</a>~          |

** Supported image types

Only the following formats are guaranteed to render inline and preview
in the floating image window:

| Format     | Inline render | Overlay preview | Inversion |
|------------|:-------------:|:---------------:|:---------:|
| `.png`     | ✅             | ✅               | ✅         |
| `.jpg` / `.jpeg` | ✅        | ✅               | ✅         |
| `.gif`     | ✅             | ✅               | ✅         |
| `.webp`    | ✅             | ✅               | ✅         |
| `.bmp`     | ✅             | ✅               | ✅         |

Other formats (`.ico`, `.tiff`, `.svg`, `.avif`, `.heic`, etc.) are not
guaranteed to render correctly. ~.svg~ works inline (browser renders it)
but fails in the overlay (~Avalonia.Media.Imaging.Bitmap~ cannot decode
SVG). Unrecognized extensions fall back to ~application/octet-stream~ in
the WebView, producing a broken image icon.

Images that fail to load (missing file, corrupt, etc.) are automatically
skipped when using ◀▶ / N/P navigation — only successfully rendered
images appear in the cycle.

* Quote & Block Rendering

** Blockquotes (`>`) — .md and .org

Lines starting with `>` render as a styled blockquote with a blue left border:

#+BEGIN_SRC markdown
> This is a quote.
> It can span multiple lines.
>
> Empty lines separate quote blocks.
#+END_SRC

** Org general blocks (`#+BEGIN_*` / `#+END_*`) — .org only

Any `#+BEGIN_*` / `#+END_*` block (except `#+BEGIN_SRC`) renders as monospace
text with a dark background. Content is shown as-is (no inline formatting).

#+BEGIN_SRC org
#+BEGIN_QUOTE
This is an org-mode quote block.
It renders as monospace with a dark background.
#+END_QUOTE
#+END_SRC

Supported block types: ~QUOTE~, ~EXAMPLE~, ~VERSE~, ~CENTER~, and any other
~#+BEGIN_<type>~ / ~#+END_<type>~ pair.

* Keyboard Shortcuts / Key Bindings

nextlearn supports three keybinding profiles: ~Vim~ (default), ~Emacs~, and ~Custom~.
Switch between them in Settings → Key Bindings.

The Vim preset ships with the app. You can also find the complete mapping below
for reference or manual setup.

** Vim preset (default)

#+BEGIN_SRC json
{
  "_help": "[... help text ...]",
  "bindings": [
    { "_comment": "Zoom in on image",       "action": "ZoomIn",       "key": "OemPlus",  "modifiers": "Control",       "context": "ImageOverlay", "textBox": true },
    { "_comment": "Zoom out on image",      "action": "ZoomOut",      "key": "OemMinus", "modifiers": "Control",       "context": "ImageOverlay", "textBox": true },
    { "_comment": "Reset image zoom",       "action": "ResetZoom",    "key": "D0",       "modifiers": "Control",       "context": "ImageOverlay", "textBox": true },
    { "_comment": "Reset image zoom",       "action": "ResetZoom",    "key": "NumPad0",  "modifiers": "Control",       "context": "ImageOverlay", "textBox": true },
    { "_comment": "Next image in overlay",  "action": "NextImage",    "key": "N",        "modifiers": "Shift",         "context": "ImageOverlay" },
    { "_comment": "Previous image",         "action": "PreviousImage","key": "P",        "modifiers": "Shift",         "context": "ImageOverlay" },
    { "_comment": "Next page",              "action": "NextPage",     "key": "N",        "modifiers": "",              "context": "Learning" },
    { "_comment": "Next page",              "action": "NextPage",     "key": "Right",    "modifiers": "",              "context": "Learning" },
    { "_comment": "Previous page",          "action": "PreviousPage", "key": "P",       "modifiers": "",              "context": "Learning" },
    { "_comment": "Previous page",          "action": "PreviousPage", "key": "Left",    "modifiers": "",              "context": "Learning" },
    { "_comment": "Scroll content down",    "action": "ScrollDown",   "key": "J",        "modifiers": "",              "context": "Learning" },
    { "_comment": "Scroll content up",      "action": "ScrollUp",     "key": "K",        "modifiers": "",              "context": "Learning" },
    { "_comment": "Scroll content left",    "action": "ScrollLeft",   "key": "H",        "modifiers": "",              "context": "Learning" },
    { "_comment": "Scroll content right",   "action": "ScrollRight",  "key": "L",        "modifiers": "",              "context": "Learning" },
    { "_comment": "Exit to home",           "action": "NavigateHome", "key": "Q",        "modifiers": "",              "context": "Learning" },
    { "_comment": "Exit to home",           "action": "NavigateHome", "key": "D",        "modifiers": "",              "context": "Learning" },
    { "_comment": "Open go-to-page dialog", "action": "OpenGoToPage", "key": "G",        "modifiers": "Control",       "context": "Learning", "textBox": true },
    { "_comment": "Go home",                "action": "NavigateHome", "key": "Q",        "modifiers": "",              "context": "Home" },
    { "_comment": "Go home",                "action": "NavigateHome", "key": "D",        "modifiers": "",              "context": "Home" },
    { "_comment": "Scroll deck list down",  "action": "ScrollDeckListDown", "key": "J", "modifiers": "",              "context": "Home" },
    { "_comment": "Scroll deck list up",    "action": "ScrollDeckListUp",   "key": "K", "modifiers": "",              "context": "Home" },
    { "_comment": "Focus search bar (/)",   "action": "FocusSearchBar", "key": "Oem2",   "modifiers": "",              "context": "Home" },
    { "_comment": "Zoom text in",           "action": "ZoomTextIn",   "key": "OemPlus",  "modifiers": "Control+Shift",  "textBox": true },
    { "_comment": "Zoom text out",          "action": "ZoomTextOut",  "key": "OemMinus", "modifiers": "Control+Shift",  "textBox": true },
    { "_comment": "Reset text zoom",        "action": "ResetTextZoom","key": "D0",       "modifiers": "Control+Shift",  "textBox": true },
    { "_comment": "Reset text zoom",        "action": "ResetTextZoom","key": "NumPad0",  "modifiers": "Control+Shift",  "textBox": true },
    { "_comment": "Open settings (Ctrl+,)", "action": "OpenSettings", "key": "OemComma", "modifiers": "Control",       "textBox": true },
    { "_comment": "Toggle shortcuts handbook", "action": "ToggleShortcutsHandbook", "key": "Oem2", "modifiers": "Shift" }
  ]
}
#+END_SRC

To use a custom config, copy the JSON above into ~/Desktop/nextlearn/keybindings.json~, rename the hidden template from the app's config directory:

#+BEGIN_SRC sh
cp ~/.config/nextlearn/keybindings.json~ ~/.config/nextlearn/keybindings.json
vi ~/.config/nextlearn/keybindings.json
#+END_SRC

Then select ~Custom~ in Settings → Key Bindings and click Save.

** Binding format reference

| Field       | Required | Description                                                  |
|-------------+----------+--------------------------------------------------------------|
| ~action~    | yes      | One of the ~KeyboardActionKind~ enum values (see below)      |
| ~key~       | yes      | Avalonia Key enum name (e.g. ~N~, ~Right~, ~OemPlus~)      |
| ~modifiers~ | yes      | Comma-separated: ~Control~, ~Shift~, ~Alt~, or empty ~""~   |
| ~context~   | no       | ~Learning~, ~Home~, ~ImageOverlay~, or omit for global       |
| ~textBox~   | no       | Set ~true~ to allow in text input fields (default false)     |
| ~_comment~  | no       | Ignored by the app — documentation only                      |

Available ~action~ values:

- ~NextPage~ / ~PreviousPage~ — flip deck pages during study
- ~ScrollDown~ / ~ScrollUp~ / ~ScrollLeft~ / ~ScrollRight~ — scroll WebView content
- ~NavigateHome~ — exit study or settings back to deck list
- ~OpenSettings~ / ~CloseSettings~ — toggle settings panel
- ~ZoomTextIn~ / ~ZoomTextOut~ / ~ResetTextZoom~ — font scaling
- ~ZoomIn~ / ~ZoomOut~ / ~ResetZoom~ — image overlay zoom
- ~NextImage~ / ~PreviousImage~ — cycle images in overlay
- ~FocusSearchBar~ — focus the search bar on home screen
- ~ScrollDeckListDown~ / ~ScrollDeckListUp~ — scroll the deck list
- ~ToggleShortcutsHandbook~ — show/hide this handbook
- ~OpenGoToPage~ — jump to a page number during study
- ~ZoomHeatmapIn~ / ~ZoomHeatmapOut~ / ~ZoomHeatmapReset~ — heatmap zoom

The following are handled internally and are not configurable:

- =Esc=: closes overlays in priority order (GoToPage → Handbook → Image → ... → clear focus)
- =g= then =i= chord: focus and clear search bar on home screen

* Known Limitations

** .org file support is partial
- Parsing exists but the editor always produces ~.md~ output
- ~GetDeckMarkdownPath()~ creates a ~.md~ fallback, potentially
  creating duplicate files when the original is ~.org~
- Heading parsing and code blocks work correctly (~*~/~~~** ~~, ~#+BEGIN_SRC~)
- Inline code in org files uses ~=code=~ / ~~code~~ syntax (not backticks)

** File-based decks bypass the database
- Deck content lives in ~.md~ files, DB stores progress/activity only
- Deck IDs are deterministic (SHA256 of path), so progress is stable
- But deck content is never persisted to DB — files are the source of truth

** Single-window architecture
- All views stack in one ~Panel~ — visibility managed by boolean flags
- ~IsLearning~, ~IsSettingsOpen~, ~IsSidebarOpen~ instead of a single enum

** Search doesn't search deck page content
- Only ~Title~, ~Description~, ~FileName~, and ~Tags~ (via ~#tag~) are searched
- The actual text content inside deck pages is not indexed or searched
